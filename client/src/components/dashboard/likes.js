// import React from 'react';
// import Proptypes from 'prop-types';
// // const func =  profile.user.likes.map(event => {
// //     profile.user.likes.map(event => {
// //       return (
// //         <span>{event._id}</span>

// //         );
// //       });
//       const Dashboard = ({
//         getCurrentProfile,
//         deleteAccount,
//         auth: { user },
//         profile: { profile, loading },
//         showevent
//       })
//   const { roomUsers, sendDM, currentUser } = props;
//   const UserLikes = profile.user.likes.map(event => {
//     return (
//       <li key={user.id}>
//         <div>
//           <span className={`presence ${user.presence.state}`} />
          
//         </div>
//         {currentUser.id !== user.id ? (
//           <button
//             onClick={() => sendDM(user.id)}
//             title={`Send ${user.name} a direct message`}
//             className="send-dm"
//           >
//             +
//           </button>
//         ) : null}
//       </li>
//     );
//   });

//   return (
//     <div>
//       <ul>{UserLikes}</ul>
//     </div>
//   );
// };

// RoomUsers.propTypes = {
//   roomUsers: Proptypes.array.isRequired,
//   sendDM: Proptypes.func.isRequired,
//   currentUser: Proptypes.object.isRequired,
// };

// export default RoomUsers;
